@page
@model IndexModel
@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@{ ViewData["Title"] = "Home page"; }

<style>

        .sidebar {
            position: fixed;
            top: 56px;
            bottom: 0;
            left: 0;
            z-index: 1000;
            display: block;
            padding: 20px;
            overflow-x: hidden;
            overflow-y: auto; /* Scrollable contents if viewport is shorter than content. */
            background-color: #f5f5f5;
            border-right: 1px solid #eee;
            width: 180px;
        }
        /*.sidebar {
        width: 180px;
    }*/
        .col-sm-8 {
            width: 100% !important;
            /*  position: fixed;*/
        }

        .nav-sidebar {
            color: #fff;
            background-color: #428bca;
        }

        .active {
            color: #fff;
            background-color: #428bca;
            padding: 2px;
        }
</style>
<div class="container">
    <div class="row" style=" width: 2021px; ">
        <div class="col-sm-4 sidebar">
            <ul class="nav nav-sidebar">
                <li class="active text-white"><a class="text-white" href="#"><i class="fas fa-tachometer-alt"></i>Dashboard</a></li>
                @*<li><a href="#">Reports</a></li>
                    <li><a href="#">Analytics</a></li>
                    <li><a href="#">Export</a></li>*@
            </ul>
        </div>
        <div class="col-sm-8 main">

            <div class="text-center">

                @if (SignInManager.IsSignedIn(User))
                {
    <div style="display: flex; justify-content: space-around; margin-top: 40px;">
        <iframe src="@Url.Page("/Chart1")" style="width: 3000px !important; height: 545px !important; background-color:#ffffff " scrolling="no"></iframe>
        @*<iframe src="@Url.Page("/Chart2")" width="400" height="400" scrolling="no"></iframe>*@
    </div>}
            </div>
        </div>
    </div>
</div>



